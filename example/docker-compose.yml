services:
  miniapp-example:
    build:
      context: ..
      dockerfile: example/Dockerfile
    ports:
      - "8080:80"
    environment:
      - APP_ID=${APP_ID:-example-app}
      - JWKS_URL=${JWKS_URL:-https://chat.cherry.fun/.well-known/jwks.json}
